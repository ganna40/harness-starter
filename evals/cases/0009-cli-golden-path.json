{
  "id": "0009",
  "title": "CLI golden path — real round-trip across processes (Postgres)",
  "tier": "behavioral",
  "description": "Spawns the CLI once to create a note, then a separate CLI process to fetch it back by id. Requires DATABASE_URL (Postgres). Exercises every layer plus persistence. Skips gracefully if DATABASE_URL is not set.",
  "type": "command",
  "command": "node evals/harness/cli-golden-path.mjs",
  "expect": {
    "exitCode": 0,
    "stdoutContains": ["ok"]
  },
  "tags": ["cli", "golden-path", "integration", "postgres"]
}
