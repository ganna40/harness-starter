{
  "id": "0004",
  "title": "No secret patterns in tracked files",
  "tier": "structural",
  "description": "check-structure scans for private keys, API keys, and tokens. This case ensures the repo stays clean. On failure: find the leaked secret, rotate it, and purge from history with a fresh commit.",
  "type": "command",
  "command": "node scripts/check-structure.mjs --json",
  "expect": {
    "exitCode": 0,
    "stdoutNotContains": ["Possible"]
  },
  "tags": ["security", "ci-gating"]
}
